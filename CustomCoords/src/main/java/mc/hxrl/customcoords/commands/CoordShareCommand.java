package mc.hxrl.customcoords.commands;

import com.mojang.brigadier.CommandDispatcher;

import net.minecraft.commands.CommandSource;
import net.minecraft.commands.Commands;

public class CoordShareCommand {
	
	public CoordShareCommand(CommandDispatcher<CommandSource> dispatcher) {
		//dispatcher.register(Commands.literal("cc").then(Commands.literal("share").executes((command) -> {
		//	return 0;
		//})));
	}
	
}
